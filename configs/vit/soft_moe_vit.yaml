experiment_name: soft_moe_vit
output_dir: ./outputs/vit_soft_moe_vit
seed: 42
model:
  type: soft_moe_vit
  num_classes: 1000
  num_experts: 32
  slots_per_expert: 1
  drop_path_rate: 0.1
algorithm:
  type: none
training:
  epochs: 100
  batch_size: 64
  grad_accum_steps: 4
  lr: 0.00025
  optimizer: adamw
  weight_decay: 0.05
  betas:
  - 0.9
  - 0.999
  lr_schedule: cosine
  warmup_epochs: 5
  label_smoothing: 0.1
  mixup_alpha: 0.8
  cutmix_alpha: 1.0
  mixup_switch_prob: 0.5
  amp_dtype: bf16
  _no_compile: true
  max_grad_norm: 1.0
data:
  dataset: imagenet
  data_dir: ./data_cache/imagenet
  num_workers: 8
  augmentation: deit
